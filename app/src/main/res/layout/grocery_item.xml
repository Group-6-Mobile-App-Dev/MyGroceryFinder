<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="12dp">

    <ImageView
        android:id="@+id/groceryImage"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_marginEnd="16dp"
        android:contentDescription="@string/grocery_image_desc"
        android:scaleType="centerCrop"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <TextView
        android:id="@+id/groceryName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Grocery Item"
        android:textStyle="bold"
        android:textSize="18sp"
        app:layout_constraintTop_toTopOf="@id/groceryImage"
        app:layout_constraintStart_toEndOf="@id/groceryImage"
        app:layout_constraintEnd_toEndOf="parent" />

    <TextView
        android:id="@+id/groceryCategory"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Category: Produce"
        android:textSize="16sp"
        app:layout_constraintTop_toBottomOf="@id/groceryName"
        app:layout_constraintStart_toEndOf="@id/groceryImage"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
